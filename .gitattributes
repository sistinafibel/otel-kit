# 줄바꿈: 저장소에는 항상 LF 로 저장한다 (core.autocrlf 설정과 무관하게 일관되도록).
* text=auto

# 셸 스크립트는 체크아웃 시에도 LF 를 유지한다 — CRLF 면 Linux 러너의 bash 가 실행하지 못한다.
*.sh text eol=lf

# 이미지 등 바이너리는 변환하지 않는다.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
