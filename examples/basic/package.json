{
  "name": "otel-kit-example-basic",
  "private": true,
  "description": "Minimal Node.js HTTP server instrumented with @cloudjun/otel-kit",
  "scripts": {
    "start": "node --env-file=.env --require @cloudjun/otel-kit/register index.js",
    "collector": "docker compose up"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@cloudjun/otel-kit": "file:../..",
    "winston": "^3.13.0"
  }
}
